{
  "name": "image_grid",
  "type": "module",
  "dependencies": {
    "lit": "^2.7.5"
  },
  "devDependencies": {
    "esbuild": "^0.12.26",
    "npm-watch": "0.11.0"
  },
  "watch": {
    "build": "src/*.js"
  },
  "scripts": {
    "build": "esbuild --bundle src/image_grid.js --outdir=dist",
    "watch": "npm-watch"
  }
}
